# Changelog

所有重要变更记录于此文件。格式参考 [Keep a Changelog](https://keepachangelog.com/)，版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [0.1.4] - 2026-08-12

### 新增功能
- **用量统计面板**：月度账单卡片、模型消耗排行（近 30 天 TOP10）、近 7 日明细、一键 CSV 导出。
- **全局快捷键**：`⌘⇧Space` 呼出/隐藏主面板，`⌘⇧O` 呼出/隐藏灵动岛，二者保持互斥。
- **灵动岛交互增强**：`Esc` 折叠卡片；余额低于阈值时红色脉冲告警；采集出错时角落红点提示。
- **托盘动态状态**：根据最新余额/采集中/采集错误动态显示托盘标题与菜单。
- **代理鉴权**：新增 `x-proxy-secret` 设置项与命令，可为本地代理开启请求鉴权。
- **每日用量聚合**：`balance_snapshots` 按天聚合，后台定期清理细粒度快照，减小数据库体积。
- **多显示器支持**：记住窗口在任意显示器上的位置，重启后恢复。
- **采集错误提示**：主面板顶部错误横幅 + Toast 提示，便于排查账户/密钥问题。
- **自动更新**：接入 `tauri-plugin-updater`，可检查并安装新版本。
- **数据层统一**：悬浮窗复用 `dashboardStore`，与面板共享同一份状态。
- **口径统一**：月度账单与「今日消耗」均使用 `displayCost`（代理真实花费优先，否则取单价估算）。

### 优化
- **构建体积**：ECharts 按需引入（tree-shaking），前端包体由约 2.1MB 降至 754KB（gzip 259KB）。
- **原子写入**：`upsertDailyUsage` 改为 `ON CONFLICT DO UPDATE` 原子 upsert，避免并发竞态。
- **查询性能**：`latestBalances` 改用 `GROUP BY MAX(id)`，替代逐账户子查询。
- **采集去重**：自动采集定时器通过共享的 `last_collect_at` 去重，面板与悬浮窗任一常驻即按时采集，不会重复采集。

### 修复
- **快捷键冲突**：`⌥Space` 在 macOS 上会抢占输入法切换，已改为 `⌘⇧Space` / `⌘⇧O`。
- **cost 双写覆盖**：余额差值计费会覆盖代理逐笔记账的真实花费，现仅对「无代理数据」的日期回填，保留代理权威值。
- **灵动岛与面板共存**：上一版允许二者同时出现（回归），已恢复互斥逻辑（含快捷键、托盘、面板关闭）。
- **毛玻璃卡片回归**：回退实验性的 vibrancy 毛玻璃，恢复为实色卡片以保证可读性。
- **重复采集**：悬浮窗与面板各自启动定时器可能重复调用采集接口，已通过时间戳去重。
- **消耗口径不一致**：修复「今日消耗 ¥0.0 但明细有金额」的问题，统一 `displayCost` 计算口径。

## [0.1.3] - 2026-08-03

- 复合图表（余额 + token 同一条时间轴）、分钟级 token 明细、自然缩放、国际化、交互修复。
